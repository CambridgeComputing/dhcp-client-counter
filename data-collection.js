// Collect data from remote servers

// Read most recent DHCP log file

// Drop header lines that just have documentation included (~32 lines?)

// Ingest CSV contents. Om nom.

// Submit to DB if not existing already
// - perhaps grab last timestamp and filter before submitting?
// - echo timestamp of activity and status (success/failure)
